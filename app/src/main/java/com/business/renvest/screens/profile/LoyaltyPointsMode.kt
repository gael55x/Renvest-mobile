package com.business.renvest.screens.profile

object LoyaltyPointsMode {
    const val PER_VISIT = "per_visit"
    const val PER_SPEND = "per_spend"
    const val MANUAL = "manual"

    val ALL = listOf(PER_VISIT, PER_SPEND, MANUAL)
}
