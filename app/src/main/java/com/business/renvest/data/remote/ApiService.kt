package com.business.renvest.data.remote

/**
 * Placeholder for future HTTP client integration (e.g. Retrofit, Ktor).
 * Keep empty until backend contracts exist.
 */
interface ApiService {
    // suspend fun fetchPrograms(): RenvestResult<List<LoyaltyProgramDto>>
}
